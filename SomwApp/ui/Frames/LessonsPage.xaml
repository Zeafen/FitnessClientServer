<Page x:Class="SomwApp.ui.Frames.LessonsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SomwApp.ui.Frames"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:rules="clr-namespace:SomwApp.presentation.rules"
      xmlns:converters="clr-namespace:SomwApp.presentation.converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LessonsPage" Background="White" DataContext="{Binding RelativeSource={RelativeSource Self}, Path=viewModel}">
    <Page.Resources>
        <converters:DateOnlyConverter x:Key="DateTimeToDateConverter"/>
        <converters:TimeOnlyConverter x:Key="DateTimeToTimeConverter"/>
    </Page.Resources>

    <Grid Margin="4,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="LessonsDGr" ItemsSource="{Binding Lessons}"  Grid.Row="1" CanUserAddRows="False"  AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="LessonsDGr_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID_Lessons}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, ConverterCulture=ru}"/>
                <DataGridTextColumn Header="Время" Binding="{Binding Time, ConverterCulture=ru}"/>
                <DataGridTextColumn Header="Количество практикантов" Binding="{Binding NumberOfPracticants}"/>
                <DataGridTextColumn Header="Тренер" Binding="{Binding Coach.FullName}"/>
            </DataGrid.Columns>
        </DataGrid>
        <GridSplitter BorderThickness="1.5" BorderBrush="Black" ResizeDirection="Columns"/>
        <TabControl Grid.Row="1" Grid.Column="1">
            <TabItem Header="Данные">
                <ScrollViewer VerticalScrollBarVisibility="Hidden" >
                    <Grid Name="InfoField" DataContext="{Binding SelectedLesson}" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.ColumnSpan="2" Padding="8" Text="Данные о тренировке" HorizontalAlignment="Center" FontSize="20" FontFamily="Sans" FontWeight="Bold"/>

                        <TextBlock Grid.Row="1" Text="Название:" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="8" FontSize="10" FontFamily="Sans" VerticalAlignment="Center" TextWrapping="NoWrap">
                            <TextBox.Text>
                                <Binding Path="Title" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <rules:NotEmptyStringRule/>
                                        <rules:LettersLimitRule Min="1" Max="100"/>
                                        <ExceptionValidationRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBlock Grid.Row="2" Text="Дата:" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" Grid.Row="2" Margin="8,0" SelectedDate="{Binding Date, Converter={StaticResource DateTimeToDateConverter}, ConverterCulture=ru}" VerticalAlignment="Center"/>


                        <TextBlock Grid.Row="3" Text="Время:" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <materialDesign:TimePicker Grid.Column="1" Grid.Row="3" Margin="8,0" SelectedTime="{Binding Time, Converter={StaticResource DateTimeToTimeConverter}, ConverterCulture=ru}" VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="4" Text="Длительность тренировки (в часах)" TextWrapping="Wrap" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" FontSize="10" FontFamily="Sans" Margin="8" VerticalAlignment="Center" TextWrapping="NoWrap" TextAlignment="Center">
                            <TextBox.Text>
                                <Binding Path="Hours" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <rules:DoubleNumValidationRule/>
                                        <ExceptionValidationRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBlock Grid.Row="5" Text="Количество практикантов" TextWrapping="Wrap" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="5" Grid.Column="1" FontSize="10" FontFamily="Sans" Margin="8" VerticalAlignment="Center" TextWrapping="NoWrap" TextAlignment="Center">
                            <TextBox.Text>
                                <Binding Path="NumberOfPracticants" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <rules:IntNumValidationRule/>
                                        <ExceptionValidationRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBlock Grid.Row="6" Text="Тренер" TextWrapping="Wrap" FontSize="14" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" Grid.Row="6" Margin="12" FontSize="10" FontFamily="Sans" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.Coaches}" SelectedItem="{Binding Coach}" DisplayMemberPath="FullName"/>

                        <StackPanel Grid.Row="7" Grid.ColumnSpan="2">
                            <Button Content="Изменить данные об оплате" Grid.Row="3" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.LessonEditCommand}" CommandParameter="{Binding ElementName=InfoField, Path=DataContext}"/>
                            <Button Content="Удалить данные об оплате" Grid.Row="4" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Click="Button_Click"/>
                            <Button Content="Добавить данные об оплате" Grid.Row="5" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.LessonAddCommand}" CommandParameter="{Binding ElementName=InfoField, Path=DataContext}"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Фильтрация">
                <StackPanel Orientation="Vertical">
                    <TextBlock Grid.ColumnSpan="2" Padding="8" Text="Атрибуты фильтрации" HorizontalAlignment="Center" FontSize="18" FontFamily="Sans" FontWeight="Bold"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Padding="8,4" Text="По тренеру" TextAlignment="Left" HorizontalAlignment="Stretch" VerticalAlignment="Center" FontSize="14" FontFamily="Sans" />
                        <ComboBox Grid.Column="1" Margin="9,0" ToolTip="Выберите тренера" HorizontalAlignment="Stretch" VerticalAlignment="Center" ItemsSource="{Binding Coaches}" SelectedItem="{Binding CoachFilter}" DisplayMemberPath="FullName"/>
                    </Grid>
                    <Separator Margin="0,12"/>
                    <TextBlock Padding="0,4" Text="По дате" TextAlignment="Center" VerticalAlignment="Center" FontSize="14" FontFamily="Sans" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock  Padding="0,4" Text="Дата с" TextAlignment="Center" VerticalAlignment="Center" FontSize="14" FontFamily="Sans" />
                        <DatePicker Grid.Column="1" Margin="8,4" VerticalAlignment="Center" SelectedDate="{Binding DateFromFilter, ConverterCulture=ru, Converter={StaticResource DateTimeToDateConverter}}"/>
                        <TextBlock Grid.Row="1" Padding="0,4" Text="Дата по" TextAlignment="Center" VerticalAlignment="Center" FontSize="14" FontFamily="Sans" />
                        <DatePicker Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="8,4" SelectedDate="{Binding DateToFilter, ConverterCulture=ru, Converter={StaticResource DateTimeToDateConverter}}"/>
                    </Grid>
                    <Separator Margin="0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Padding="8,4" Text="По назначенному времени" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Center" FontSize="14" FontFamily="Sans" />
                        <materialDesign:TimePicker Grid.Column="1" Margin="8,4" ToolTip="Выберите время" HorizontalAlignment="Stretch" VerticalAlignment="Center" SelectedTime="{Binding TimeFilter, Converter={StaticResource DateTimeToTimeConverter}, ConverterCulture=ru}"/>
                    </Grid>
                    <Button Content="Сбросить фильтры" Grid.Row="3" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.ClearFiltersCommand}"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
