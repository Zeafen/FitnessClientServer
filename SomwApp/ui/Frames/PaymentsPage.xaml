<Page x:Class="SomwApp.ui.Frames.PaymentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SomwApp.ui.Frames"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:rules="clr-namespace:SomwApp.presentation.rules"
      xmlns:converters="clr-namespace:SomwApp.presentation.converters"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="PaymentsPage" Background="White" DataContext="{ Binding RelativeSource={RelativeSource Self}, Path=viewModel}">
    <Page.Resources>
        <converters:DateOnlyConverter x:Key="DateTimeToDateConverter"/>
    </Page.Resources>

    <Grid Margin="4,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.RowSpan="2" Padding="8" Text="Фильрация по" VerticalAlignment="Center" TextAlignment="Center" FontSize="16" FontFamily="Sans" FontWeight="Bold"/>

                <TextBlock Grid.Column="1" Padding="0,4" Text="Сотруднику" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="14" FontFamily="Sans" />
                <ComboBox Grid.Column="1" Margin="12,0,12,0" Grid.Row="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding CustomerFilter}" DisplayMemberPath="FullName" VerticalAlignment="Top"/>

                <TextBlock Grid.Column="2" Padding="0,4" Text="Подписке" TextAlignment="Center" HorizontalAlignment="Center" FontSize="14" FontFamily="Sans" />
                <ComboBox Grid.Column="2" Margin="12,0,12,8" Grid.Row="1" ItemsSource="{Binding Subscriptions}" SelectedItem="{Binding SubscriptionFilter}" DisplayMemberPath="TypeofSubscription"/>


            </Grid>
            <DataGrid Name="PaymentsDGr" ItemsSource="{Binding Payments}" Grid.Row="1" CanUserAddRows="False"  AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="PaymentsDGr_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID_Payment}"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding PaymentDate, ConverterCulture=ru}"/>
                    <DataGridTextColumn Header="Дата истечения срока" Binding="{Binding ValidityEndDate, ConverterCulture=ru}"/>
                    <DataGridTextColumn Header="Стоимость" Binding="{Binding Amount}"/>
                    <DataGridTextColumn Header="Клиент" Binding="{Binding Customer.FullName}"/>
                    <DataGridTextColumn Header="Подписка" Binding="{Binding Subscription.TypeofSubscription}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <GridSplitter BorderThickness="1.5" BorderBrush="Black" ResizeDirection="Columns"/>
        <ScrollViewer  Grid.Row="1" Grid.Column="1" >
            <Grid Name="InfoField" DataContext="{Binding SelectedPayment}" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition Height="1.5*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.ColumnSpan="2" Padding="8" Text="Данные об оплате" HorizontalAlignment="Center" FontSize="20" FontFamily="Sans" FontWeight="Bold"/>

                <TextBlock Grid.Row="1" Text="Клиент:" FontSize="16" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" Grid.Row="1" Margin="12" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.Customers}" SelectedItem="{Binding Customer}" DisplayMemberPath="FullName"/>

                <TextBlock Grid.Row="2" Text="Подписка:" FontSize="16" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" Grid.Row="2" Margin="12" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.Subscriptions}" SelectedItem="{Binding Subscription}" DisplayMemberPath="TypeofSubscription"/>

                <TextBlock Grid.Row="3" Text="Дата оплаты:" FontSize="16" FontFamily="Sans" Padding="8" TextAlignment="Center" VerticalAlignment="Center"/>
                <DatePicker Grid.Column="1" Grid.Row="3" Margin="12" SelectedDate="{Binding PaymentDate, Converter={StaticResource DateTimeToDateConverter}, ConverterCulture=ru}" VerticalAlignment="Center"/>

                <StackPanel Grid.Row="4" Grid.ColumnSpan="2">

                    <Button Content="Изменить данные об оплате" Grid.Row="3" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.PaymentEditCommand}" CommandParameter="{Binding ElementName=InfoField, Path=DataContext}"/>
                    <Button Content="Удалить данные об оплате" Grid.Row="4" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Click="Button_Click"/>
                    <Button Content="Добавить данные об оплате" Grid.Row="5" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="8" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.PaymentAddCommand}" CommandParameter="{Binding ElementName=InfoField, Path=DataContext}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
